# Railway Nixpacks Configuration
# This tells Railway to install FFmpeg for video processing

[phases.setup]
nixPkgs = ['ffmpeg']    ← ONLY FFmpeg (Railway handles Node.js)

[phases.install]
dependsOn = ['setup']

[start]
cmd = 'npm start'
